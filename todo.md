Add a command to list all kegs of beer we have in stock. You can find it from the article name, the format is [type]/[package]/[volume in litres]/[name]/[alcohol by volume]. The naming is in swedish so type should be "öl" (beer) and package should be "fat" (keg). Concatenate name and ABV and use it as description in. The kegs in stock should be listed as description, package volume (20 or 30 liter) and number of kegs in stock.